# This file was auto-generated by the Firebase CLI and updated for hosting-only preview deployments
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting Preview on PR
on: pull_request

permissions:
  checks: write
  contents: read
  pull-requests: write

env:
  NODE_VERSION: '22'

jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build React app
        run: npm run build
        env:
          NODE_ENV: production
          VITE_SONIOX_API_KEY: ${{ secrets.VITE_SONIOX_API_KEY }}
      
      - name: Deploy Preview to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHABADSYNCK }}
          projectId: shabadsynck
          expires: 7d
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks

# This file was auto-generated by the Firebase CLI and updated for hosting-only deployment
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

env:
  NODE_VERSION: '22'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build React app
        run: npm run build
        env:
          NODE_ENV: production
          VITE_SONIOX_API_KEY: ${{ secrets.VITE_SONIOX_API_KEY }}
      
      # Deploy Hosting using Firebase Action
      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHABADSYNCK }}
          channelId: live
          projectId: shabadsynck
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
